## Crear una archivo `config.py` con este formato:

```
DB_CONNECTIONS = {
    "engine_sf": "postgresql://postgres:1234@localhost/ENERSA",
    "engine_cubo": "postgresql://postgres:1234@localhost/DW_ENERSA"
}
```